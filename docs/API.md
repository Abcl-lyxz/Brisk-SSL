# Brisk-SSL API index

Generated by `tools/apidoc.py` from [`include/brisk.h`](../include/brisk.h) - do not edit. The header is the reference: follow a link for the full contract. What each error means in the field and what to do about it: [TROUBLESHOOTING.md](TROUBLESHOOTING.md).

## Errors

| | name | summary |
|---|---|---|
| error | [`BRISK_OK (0)`](../include/brisk.h#L41) | (see the header) |
| error | [`BRISK_E_ARG (-1)`](../include/brisk.h#L42) | invalid argument: unknown algorithm, length out of range, ... |
| error | [`BRISK_E_RNG (-2)`](../include/brisk.h#L43) | kernel randomness unavailable; seccomp filters must allow getrandom |
| error | [`BRISK_E_AUTH (-3)`](../include/brisk.h#L44) | a cryptographic check failed on data the peer sent: AEAD authentication (TLS bad_record_mac; no plaintext released), a signature that does not verify (TLS de... |
| error | [`BRISK_E_PROTO (-4)`](../include/brisk.h#L48) | the peer violated the TLS protocol: a message out of order, a bad length, a field or extension the RFC forbids. |
| error | [`BRISK_E_PEER_ALERT (-5)`](../include/brisk.h#L53) | the peer ended the connection with a fatal TLS alert (RFC 9846 6.2; its AlertDescription is kept on the connection, see brisk_alert). |
| error | [`BRISK_E_IO (-6)`](../include/brisk.h#L64) | the network failed (blocking API only): DNS, socket or connect error, a send/recv error, the peer's TCP FIN before its close_notify (a truncation, RFC 9846 6... |
| error | [`BRISK_E_TIMEOUT (-7)`](../include/brisk.h#L69) | a blocking call waited cfg.timeout_ms without progress. |
| error | [`BRISK_E_WANT (-8)`](../include/brisk.h#L76) | sans-I/O only, and not a failure: "feed me more bytes first" - brisk_status while the handshake is still running, brisk_app_read when no application data has... |
| error | [`BRISK_E_RETRY (-9)`](../include/brisk.h#L79) | brisk_h2_* / brisk_h3_* only: the server guaranteed it did NOT process this request (RFC 9113 8.7: RST_STREAM REFUSED_STREAM or a stream above a GOAWAY's las... |
| error | [`BRISK_E_INSECURE (-10)`](../include/brisk.h#L85) | the server speaks the protocol correctly but only below this library's security floor, which no setting lowers: TLS 1.2 without extended_main_secret (RFC 762... |
| function | [`brisk_version`](../include/brisk.h#L96) | Library version, e.g. "0.1.0-dev". |
| function | [`brisk_build_info`](../include/brisk.h#L100) | What this binary was compiled with, e.g. "0.1.0-dev profile=DEFAULT". |

## Hashes

| | name | summary |
|---|---|---|
| macro | [`BRISK_SHA256_LEN`](../include/brisk.h#L108) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| macro | [`BRISK_SHA384_LEN`](../include/brisk.h#L109) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| macro | [`BRISK_SHA512_LEN`](../include/brisk.h#L110) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| type | [`brisk_sha256_ctx`](../include/brisk.h#L117) | (see the header) |
| type | [`brisk_sha512_ctx`](../include/brisk.h#L123) | (see the header) |
| type | [`brisk_sha384_ctx`](../include/brisk.h#L125) | (see the header) |
| function | [`brisk_sha256_init`](../include/brisk.h#L127) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha256_update`](../include/brisk.h#L128) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha256_final`](../include/brisk.h#L129) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha256`](../include/brisk.h#L130) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha384_init`](../include/brisk.h#L132) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha384_update`](../include/brisk.h#L133) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha384_final`](../include/brisk.h#L134) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha384`](../include/brisk.h#L135) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha512_init`](../include/brisk.h#L137) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha512_update`](../include/brisk.h#L138) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha512_final`](../include/brisk.h#L139) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |
| function | [`brisk_sha512`](../include/brisk.h#L140) | SHA-256, SHA-384, SHA-512 (FIPS 180-4). |

## HMAC

| | name | summary |
|---|---|---|
| type | [`brisk_hash_alg`](../include/brisk.h#L146) | (see the header) |
| function | [`brisk_hash_len`](../include/brisk.h#L149) | Digest length in bytes of `alg`, or 0 if `alg` is not a valid brisk_hash_alg. |
| type | [`brisk_hash_ctx`](../include/brisk.h#L154) | (see the header) |
| type | [`brisk_hmac_ctx`](../include/brisk.h#L159) | (see the header) |
| function | [`brisk_hmac_init`](../include/brisk.h#L162) | Returns BRISK_OK, or BRISK_E_ARG for an unknown algorithm. |
| function | [`brisk_hmac_update`](../include/brisk.h#L164) | (see the header) |
| function | [`brisk_hmac_final`](../include/brisk.h#L166) | Writes brisk_hash_len(alg) bytes and wipes the context. |
| function | [`brisk_hmac`](../include/brisk.h#L167) | (see the header) |

## Client-certificate signing hook

| | name | summary |
|---|---|---|
| macro | [`BRISK_SIG_ECDSA_P256_LEN`](../include/brisk.h#L180) | (see the header) |

## Session resumption

| | name | summary |
|---|---|---|
| macro | [`BRISK_TICKET_MAX`](../include/brisk.h#L207) | (see the header) |

## TLS 1.3 client connection

| | name | summary |
|---|---|---|
| type | [`brisk_cfg`](../include/brisk.h#L279) | (see the header) |
| macro | [`BRISK_DEFAULTS`](../include/brisk.h#L281) | (see the header) |
| type | [`brisk_conn`](../include/brisk.h#L285) | One connection. |

## TLS 1.3 client connection - blocking API (Linux)

| | name | summary |
|---|---|---|
| function | [`brisk_connect`](../include/brisk.h#L299) | Resolve `host`, connect over TCP (IPv4 or IPv6, each address in turn) and complete the TLS 1.3 handshake: returns once the server is authenticated and our la... |
| function | [`brisk_read`](../include/brisk.h#L308) | Up to min(cap, INT_MAX) bytes of application data. |
| function | [`brisk_write`](../include/brisk.h#L312) | Send all `len` bytes, or fail: BRISK_OK or < 0. |
| function | [`brisk_close`](../include/brisk.h#L316) | Send close_notify (best effort, waiting at most 1 s), close the socket, wipe and free everything. |

## TLS 1.3 client connection - sans-I/O API

| | name | summary |
|---|---|---|
| function | [`brisk_conn_size`](../include/brisk.h#L330) | Bytes of memory brisk_conn_init needs (any alignment). |
| function | [`brisk_conn_init`](../include/brisk.h#L338) | Set up a connection in mem[0..mem_len) (mem_len >= brisk_conn_size()) and queue the ClientHello. |
| function | [`brisk_feed`](../include/brisk.h#L345) | Take bytes received from the server, in any split. |
| function | [`brisk_pull`](../include/brisk.h#L351) | Bytes to send to the server: whole TLS records only, as many as fit in cap. |
| function | [`brisk_status`](../include/brisk.h#L354) | BRISK_OK once connected, BRISK_E_WANT while handshaking, else the sticky error. |
| function | [`brisk_app_read`](../include/brisk.h#L358) | Received application data: BRISK_OK with *n > 0 bytes; BRISK_OK with *n == 0 = the server sent close_notify (clean end); BRISK_E_WANT = nothing yet; else the... |
| function | [`brisk_app_write`](../include/brisk.h#L364) | Encrypt application data into out[0..cap): *used = plaintext bytes taken, *out_len = bytes to send (it may also carry owed handshake records). |
| function | [`brisk_close_notify`](../include/brisk.h#L370) | Queue close_notify (RFC 9846 6.1) for brisk_pull. |
| function | [`brisk_alert`](../include/brisk.h#L374) | The TLS AlertDescription that ended the connection - the one we sent, or the server's (with BRISK_E_PEER_ALERT; 0 then means close_notify) - or 0 while nothi... |
| function | [`brisk_alpn`](../include/brisk.h#L378) | The ALPN protocol the server selected: *name (not NUL-terminated, valid while c lives) and *len; *len == 0 = none. |
| function | [`brisk_resumed`](../include/brisk.h#L382) | 1 if the handshake resumed a session from cfg.ticket (no certificate was checked on this connection: the ticket vouches for the server it was issued by), els... |
| function | [`brisk_tls_version`](../include/brisk.h#L393) | The protocol version once the handshake completed: 0x0304 (TLS 1.3) or 0x0303 (TLS 1.2, only in a build with BRISK_ENABLE_TLS12, when the server does not spe... |
| function | [`brisk_conn_wipe`](../include/brisk.h#L398) | Wipe every key, secret and buffer of a sans-I/O connection; afterwards the memory given to brisk_conn_init is all zero again (bar bytes it held before init t... |

## HTTP/2 client

| | name | summary |
|---|---|---|
| type | [`brisk_h2`](../include/brisk.h#L438) | (see the header) |
| type | [`brisk_h2_stream`](../include/brisk.h#L439) | (see the header) |
| type | [`brisk_h2_header`](../include/brisk.h#L444) | (see the header) |
| function | [`brisk_h2_size`](../include/brisk.h#L454) | Bytes of memory brisk_h2_open needs (any alignment). |
| function | [`brisk_h2_open`](../include/brisk.h#L462) | Start HTTP/2 on c (from brisk_connect, whose ALPN answer is "h2"): send the connection preface and our SETTINGS, then wait for the server's SETTINGS. |
| function | [`brisk_h2_request`](../include/brisk.h#L480) | Send one request: :method = method (a token; CONNECT is refused), :scheme https, :authority, :path = path ("/..." or "*" for OPTIONS), then hdrs[0..n) in ord... |
| function | [`brisk_h2_response`](../include/brisk.h#L487) | Wait for the final response HEADERS of s (1xx interim responses are skipped): *status is the :status (200..599), each header field goes to fn(ctx, ...) (fn m... |
| function | [`brisk_h2_read`](../include/brisk.h#L492) | Response content after brisk_h2_response: > 0 bytes (at most min(cap, INT_MAX)), 0 = the stream ended and matched any content-length, or < 0. |
| function | [`brisk_h2_stream_close`](../include/brisk.h#L496) | Release the stream's slot: RST_STREAM CANCEL when either direction is still open, unread data dropped (its flow-control credit returned). |
| function | [`brisk_h2_close`](../include/brisk.h#L500) | Send GOAWAY (NO_ERROR, best effort) and wipe all of mem. |

## QUIC v1 client

| | name | summary |
|---|---|---|
| macro | [`BRISK_QUIC_DGRAM_MAX`](../include/brisk.h#L529) | (see the header) |
| type | [`brisk_quic`](../include/brisk.h#L531) | (see the header) |

## QUIC v1 client - sans-I/O

| | name | summary |
|---|---|---|
| function | [`brisk_quic_size`](../include/brisk.h#L547) | Bytes of memory brisk_quic_init needs (any alignment). |
| function | [`brisk_quic_init`](../include/brisk.h#L554) | Set up a connection in mem[0..mem_len) and queue the first Initial. |
| function | [`brisk_quic_feed`](../include/brisk.h#L560) | One whole received UDP datagram (0..65527 bytes). |
| function | [`brisk_quic_pull`](../include/brisk.h#L565) | The next datagram to send, at most BRISK_QUIC_DGRAM_MAX bytes; 0 = nothing now (also when cap < BRISK_QUIC_DGRAM_MAX). |
| function | [`brisk_quic_deadline`](../include/brisk.h#L569) | When to call brisk_quic_pull again at the latest: absolute monotonic ms, INT64_MAX = no timer. |
| function | [`brisk_quic_status`](../include/brisk.h#L572) | BRISK_OK once the handshake completed, BRISK_E_WANT while it runs, else the sticky error. |
| function | [`brisk_quic_error`](../include/brisk.h#L576) | The QUIC error code that ended the connection (RFC 9000 20; 0x0100 + a TLS alert for TLS failures, RFC 9001 4.8; the server's code on its close), 0 while alive. |
| function | [`brisk_quic_alpn`](../include/brisk.h#L579) | As brisk_alpn / brisk_resumed, once established (BRISK_E_ARG / 0 before). |
| function | [`brisk_quic_resumed`](../include/brisk.h#L580) | (see the header) |
| function | [`brisk_quic_wipe`](../include/brisk.h#L584) | Wipe every key, secret and buffer; the memory is all zero afterwards (bar alignment slack). |

## QUIC v1 client - blocking (Linux)

| | name | summary |
|---|---|---|
| function | [`brisk_quic_connect`](../include/brisk.h#L591) | (see the header) |
| function | [`brisk_quic_close`](../include/brisk.h#L600) | Close immediately with an application error code (RFC 9000 10.2; before the handshake completed the code is replaced by APPLICATION_ERROR, 10.2.3). |
| function | [`brisk_quic_poll`](../include/brisk.h#L606) | Blocking handle: drive the connection for ms milliseconds - send what is owed, receive, run the timers - with no stream call pending; e.g. to collect a NewSe... |

## QUIC v1 client - streams (RFC 9000 2-4)

| | name | summary |
|---|---|---|
| function | [`brisk_quic_stream_open`](../include/brisk.h#L620) | Open a stream (bidi != 0: bidirectional; else unidirectional, send-only). |
| function | [`brisk_quic_stream_write`](../include/brisk.h#L627) | Queue buf[0..n) on stream id; fin = end the stream once all n bytes are taken. |
| function | [`brisk_quic_stream_read`](../include/brisk.h#L635) | Up to min(cap, INT_MAX) bytes of stream id: > 0 bytes, 0 = the server's FIN (the slot is freed once both directions are done; later calls are BRISK_E_ARG), B... |
| function | [`brisk_quic_stream_accept`](../include/brisk.h#L641) | The next stream the server opened: BRISK_OK and *id, BRISK_E_WANT / BRISK_E_TIMEOUT, or the sticky error. |

## HTTP/3 client

| | name | summary |
|---|---|---|
| type | [`brisk_h3`](../include/brisk.h#L682) | (see the header) |
| type | [`brisk_h3_stream`](../include/brisk.h#L683) | (see the header) |
| function | [`brisk_h3_size`](../include/brisk.h#L688) | Bytes of memory brisk_h3_open needs (any alignment). |
| function | [`brisk_h3_open`](../include/brisk.h#L695) | Start HTTP/3 on q (from brisk_quic_connect, whose ALPN answer is "h3"): open our control stream and send our SETTINGS (RFC 9114 6.2.1). |
| function | [`brisk_h3_request`](../include/brisk.h#L713) | Send one request on a new stream: :method (a token; CONNECT is refused), :scheme https, :authority (the host q was opened with, IPv6 in brackets, ":port" unl... |
| function | [`brisk_h3_response`](../include/brisk.h#L720) | Wait for the final response header section of s (1xx interim responses are skipped): *status is the :status (200..599), each field goes to fn(ctx, ...) (fn m... |
| function | [`brisk_h3_read`](../include/brisk.h#L725) | Response content after brisk_h3_response: > 0 bytes (at most min(cap, INT_MAX)), 0 = the stream ended and matched any content-length, or < 0. |
| function | [`brisk_h3_stream_close`](../include/brisk.h#L730) | Release the request slot. |
| function | [`brisk_h3_close`](../include/brisk.h#L735) | Wipe all of mem. |
